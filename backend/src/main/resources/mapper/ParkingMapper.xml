<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.property.mapper.ParkingMapper">
    <select id="findAll" resultType="com.property.entity.ParkingSpace">
        SELECT p.*, c.name AS community_name, u.unit_number
        FROM parking_spaces p
        JOIN communities c ON p.community_id = c.id
        LEFT JOIN units u ON p.unit_id = u.id
        <where>
            <if test="communityId != null">AND p.community_id = #{communityId}</if>
            <if test="status != null and status != ''">AND p.status = #{status}</if>
        </where>
        ORDER BY p.id DESC
    </select>
    <select id="findById" resultType="com.property.entity.ParkingSpace">
        SELECT * FROM parking_spaces WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO parking_spaces (community_id, space_number, type, status, unit_id, monthly_fee)
        VALUES (#{communityId}, #{spaceNumber}, #{type}, #{status}, #{unitId}, #{monthlyFee})
    </insert>
    <update id="update">
        UPDATE parking_spaces SET space_number=#{spaceNumber}, type=#{type}, status=#{status},
        unit_id=#{unitId}, monthly_fee=#{monthlyFee} WHERE id=#{id}
    </update>
    <delete id="deleteById">
        DELETE FROM parking_spaces WHERE id = #{id}
    </delete>
</mapper>
